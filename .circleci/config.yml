version: 2.1

parameters:
  p:
    type: integer
    default: 1

jobs:
  build:
    docker:
      - image: cimg/base:2023.11
    parallelism: << parameters.p >>
    parameters:
      p:
        type: integer
    steps:
      - checkout

workflows:
  version: 2
  workflow:
    jobs:
      - build:
          p: << pipeline.parameters.p >>
